{
  "name": "kinesis-sdk",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "description": "Kinesis SDK monorepo — DynamicNFT gaming SDK for the XRPL with pluggable storage and IPFS adapters, plus a self-hostable HTTP server.",
  "homepage": "https://github.com/Aaditya-T/Kinesis-SDK#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Aaditya-T/Kinesis-SDK.git"
  },
  "bugs": {
    "url": "https://github.com/Aaditya-T/Kinesis-SDK/issues"
  },
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:sdk": "pnpm -r --filter \"./lib/xrpl-gaming-*\" run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --if-present run typecheck"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  },
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "keywords": [],
  "author": "",
  "type": "commonjs"
}
